import { ReviewFormatterPipe } from './review-formatter.pipe';

describe('ReviewFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new ReviewFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
